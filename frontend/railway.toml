[build]
builder = "nixpacks"

[deploy]
# npm run expands $PORT; a raw "serve ... $PORT" start can run without a shell and leave $PORT literal
startCommand = "npm run serve:prod"
healthcheckPath = "/"
restartPolicyType = "on_failure"
